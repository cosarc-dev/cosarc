name: cosarc
description: "Cosarc - Cinematic Fitness & Discipline"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 🏥 The Unity Bridge (DO NOT CHANGE THIS VERSION)
  flutter_unity_widget: ^2022.2.1

  # 🌌 Cosmos UI Essentials (From Atharva)
  google_fonts: ^6.2.1
  video_player: ^2.8.2
  
  # 🧍‍♂️ 3D GLB viewer (Optional, but kept for compatibility)
  webview_flutter: ^4.0.0

  # Icons
  cupertino_icons: ^1.0.6

  http: ^1.1.0                # To talk to the Open Food Facts API
  hive: ^2.2.3                # The Database
  hive_flutter: ^1.1.0        # Hive helpers for Flutter
  path_provider: ^2.1.1       # To find where to save the DB on the phone
  intl: ^0.18.1               # For date formatting

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
  # 🚀 App launcher icon generator
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    # From Atharva's Repo
    - assets/backgrounds/
    - assets/icons/
    - assets/models/
    - assets/backgrounds/cosarc_intro.mp4
    # Note: gym_bg.jpg removed as it is now inside Unity

# 🎯 LAUNCHER ICON CONFIG
flutter_icons:
  android: true
  ios: true
  image_path: assets/icons/icon_grey.png