import 'package:flutter/material.dart';

class NutriwaveScreen extends StatelessWidget {
  const NutriwaveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("nutriwave (food ui)", style: TextStyle(color: Colors.white)),
    );
  }
}
